{
  "name": "joblab",
  "version": "0.0.1",
  "description": "A lightweight backend service that accepts jobs, processes them, and tracks their status. It exposes endpoints for CPU-heavy and I/O-heavy tasks.",
  "license": "ISC",
  "author": "Thomas Mechessa",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/bootstrap.ts",
    "build": "tsc",
    "start": "node dist/bootstrap.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "swagger-ui-express": "^5.0.0",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.5.0",
    "@types/swagger-ui-express": "^4.1.8",
    "ts-node-dev": "^2.0.0",
    "typescript": "^6.0.2"
  }
}
